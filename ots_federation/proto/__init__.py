# Generated protobuf stubs for TAK Server FIG (Federation Interface Guidance) protocol.
# Source: vendor/tak-server-official commit 5187abd
# Regenerate with: ./regen.sh (from this directory)
# The generated stubs (fig_pb2.py, binarypayload_pb2.py) use bare module imports
# (e.g. `import binarypayload_pb2`) as produced by grpc_tools.protoc without
# a --grpc_python_out package prefix. We add the proto directory to sys.path
# here so they resolve correctly when imported as taky.cot.federation.proto.fig_pb2.

import os
import sys

_PROTO_DIR = os.path.dirname(__file__)
if _PROTO_DIR not in sys.path:
    sys.path.insert(0, _PROTO_DIR)
